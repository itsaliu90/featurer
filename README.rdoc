== README

featurer is an application that helps gather feature requests from across an organization and facilitates transparency around their priority and status.

* Setup

1. Make sure Mongo is running
2. `bundle install`
3. `bin/rails server`
